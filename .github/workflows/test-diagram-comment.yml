name: Test Uploading Mermaid Diagrams

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    name: Example Diagrams
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: miketrebilcock/python-code-visualiser@beta-2
      with:
        gh-token: ${{ secrets.gh_token }}